package com.mall.userservice.config.database;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class DatabaseConfigTests {
/*
    private static final Log log = LogFactory.getLog(DatabaseConfigTests.class);


    @Test
    public void init() {

        ConnectionFactory connectionFactory = ConnectionFactories.get("r2dbc:h2:mem:///testdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE");
        DatabaseClient client = DatabaseClient.create(connectionFactory);
        DatabaseClient.GenericExecuteSpec sql = client.sql("select 1 from dual");
        System.out.println("==>" + sql.fetch().toString());

    }

*/

}
